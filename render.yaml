services:
  - type: web
    name: wardrobe-mvp-server
    runtime: node
    rootDir: server
    branch: main
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: DATABASE_PATH
        value: ./data/wardrobe.sqlite
